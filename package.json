{
  "name": "baluarte-shell",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Domínio do Projeto Nexus Baluarte — Casca visual: layout, sidebar, header, overlay, tokens, efeitos e as páginas institucionais que moram no shell",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "verificar": "node scripts/verificar-contrato.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  },
  "engines": {
    "node": "24.x"
  }
}
